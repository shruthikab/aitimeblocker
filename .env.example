# AWS Cognito (required for authentication)
NEXT_PUBLIC_COGNITO_CLIENT_ID=your-cognito-app-client-id
NEXT_PUBLIC_COGNITO_DOMAIN=https://your-domain.auth.us-east-2.amazoncognito.com

# AWS API Gateway (required for AI plan generation and task parsing)
NEXT_PUBLIC_API_BASE_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/dev

# App URL (used for Cognito logout redirect)
NEXT_PUBLIC_APP_URL=http://localhost:3000
